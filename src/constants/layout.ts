export const LAYOUT = {
  /** Intrinsic tab bar height (no safe-area inset). Add useSafeAreaInsets().bottom at call sites. */
  TAB_BAR_HEIGHT: 56,
  /** Width of the center spacer tab (FAB slot). Multiple of 8 for spacing scale. */
  TAB_SPACER_WIDTH: 88,
} as const;
